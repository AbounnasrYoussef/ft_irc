#ifndef CLIENT_HPP
#define CLIENT_HPP
#include <iostream>

// void removeClient(struct pollfd fds[], Client* clients[], int& num_fds, int index);

class Client {
    public:
        Client(){}
        ~Client(){}

};
#endif // CLIENT_HPP